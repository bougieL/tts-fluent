export enum BadanmuState {
  loading,
  connected,
  disconnected,
}

export enum BadanmuType {
  heartbeat,
  connect,
  disconnect,
  message,
}

export enum DanmuType {
  comment = 'comment',
  gift = 'gift',
  system = 'system',
}
